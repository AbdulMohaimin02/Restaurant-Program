const {Restaurant} = require('../classes/restaurant')
const {Menu} = require('../classes/menu')



describe('Reataurant class tests', () => {

    // const restaurant = new Restaurant("Nobu")
    test('dummy test to remove error', () => {
        expect('this string').toEqual('this string')
    })


    // test('New restaurant has an ID', () => {
    //     expect(restaurant.id).toBe(1)
    // })

    // test('Can access length restaurants', () => {
    //     expect(Restaurant.all.length).toBe(1)
    // })

    // test('Can read all restaurants', () => {
    //     expect(restaurant.showAllRestaurants()).toEqual([{'id': 1, 'name' : 'Nobu'}])
    // })

    // test('adding a menu to a restaurant', () => {
    //     const expensiveMenu = new Menu('expensiveMenu')
    //     restaurant.addMenu(expensiveMenu)
    // })

}) 


